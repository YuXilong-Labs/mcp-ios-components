# 迁移映射表模板

| 重复实现点 | 旧调用/API | 目标组件 | 目标 API | 关键差异 | 风险 | 批次 | 验证项 |
|---|---|---|---|---|---|---|---|

## 填写要求

- `关键差异` 至少覆盖：参数/返回值/线程/错误处理（按适用项填写）
- `风险` 建议使用：高 / 中 / 低
- `批次` 必须可执行，不要写“全量一次性替换”
- `验证项` 至少包含 1 个功能验证 + 1 个错误路径验证（关键场景）
